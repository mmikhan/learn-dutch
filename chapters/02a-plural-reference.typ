#pagebreak()

= Plural reference

Use this page as a quick lookup for common plural patterns.

== `-en` plurals

#let plural-en-table = table(
  columns: 3,
  table.header[Singular][Plural][Meaning],
  [boek], [boeken], [book],
  [dag], [dagen], [day],
  [fiets], [fietsen], [bike],
  [student], [studenten], [student],
  [zoen], [zoenen], [kiss],
)

#figure(caption: [Common nouns with `-en` plurals], plural-en-table)

== `-s` plurals

#let plural-s-table = table(
  columns: 3,
  table.header[Singular][Plural][Meaning],
  [tafel], [tafels], [table],
  [kamer], [kamers], [room],
  [meisje], [meisjes], [girl],
  [broer], [broers], [brother],
  [appel], [appels], [apple],
)

#figure(caption: [Common nouns with `-s` plurals], plural-s-table)

== unstressed `-en` + `-s`

#let unstressed-en-table = table(
  columns: 3,
  table.header[Singular][Plural][Meaning],
  [jongen], [jongens], [boy],
  [varken], [varkens], [pig],
  [keuken], [keukens], [kitchen],
)

#figure(
  caption: [Words ending in an unstressed `-en` syllable],
  unstressed-en-table,
)

== apostrophe + `s`

#let apostrophe-s-table = table(
  columns: 3,
  table.header[Singular][Plural][Meaning],
  [auto], [auto's], [car],
  [foto], [foto's], [photo],
  [menu], [menu's], [menu],
)

#figure(caption: [Common nouns that add apostrophe + `s`], apostrophe-s-table)

== common irregular plurals

#let irregular-plural-table = table(
  columns: 3,
  table.header[Singular][Plural][Meaning],
  [kind], [kinderen], [child],
  [ei], [eieren], [egg],
  [stad], [steden], [city],
)

#figure(caption: [High-frequency irregular plurals], irregular-plural-table)
