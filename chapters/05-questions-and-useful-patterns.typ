= Questions and useful sentence patterns

If you know how to ask simple questions and reuse a few high-frequency patterns, you can start speaking much earlier.

== Yes-no questions

For yes-no questions, put the conjugated verb first. Here, conjugated just means the verb is already changed to match the subject.

- `Woon je in Nederland?`
- `Hebben jullie honger?`
- `Is hij thuis?`

With modal verbs, only the first verb is conjugated. The second verb goes to the end and stays in the infinitive.

- `Kan ik u helpen?` -> can I help you?
- `Wil je iets drinken?` -> do you want something to drink?

Beginner rule: conjugated verb first, infinitive at the end.

- `Ik help u.` -> one verb, so `help` is conjugated
- `Ik kan u helpen.` -> two verbs, so `kan` is conjugated and `helpen` stays whole

So in `Kan ik u helpen?`, `kan` is the singular present-tense form of `kunnen`, and `helpen` stays an infinitive at the end.

== Question words

#let question-table = table(
  columns: 2,
  table.header[Dutch][Meaning],
  [wie], [who],
  [wat], [what],
  [waar], [where],
  [wanneer], [when],
  [waarom], [why],
  [hoe], [how],
)

#figure(caption: [Basic question words], question-table)

Examples:

- `Waar woon je?`
- `Hoe heet jij?`
- `Waarom leer je Nederlands?`

== Useful sentence frames

These patterns are worth learning early because you can reuse them every day.

- `Ik ben ...` -> `Ik ben moe.` / `Ik ben student.`
- `Ik heb ...` -> `Ik heb tijd.` / `Ik heb een vraag.`
- `Ik wil ...` + infinitive -> `Ik wil Nederlands leren.`
- `Ik kan ...` + infinitive -> `Ik kan morgen komen.`
- `Ik moet ...` + infinitive -> `Ik moet nu gaan.`
- `Ik ga naar ...` -> `Ik ga naar mijn werk.`
- `Er is ...` / `Er zijn ...` -> `Er is koffie.` / `Er zijn veel mensen.`

With modal verbs such as `willen`, `kunnen`, and `moeten`, the second verb usually goes to the end:

That second verb stays in the infinitive, the basic dictionary form.

So the pattern is:

*conjugated modal verb + infinitive*

- `Ik wil een boek kopen.`
- `Wij kunnen morgen komen.`
- `Hij moet vroeg werken.`

The same pattern appears in questions:

- `Kan ik u helpen?`
- `Kunnen jullie morgen komen?`
- `Moet hij vroeg werken?`

== A simple study habit

Take one frame and make five new sentences with your own words. For example:

- `Ik wil koffie drinken.`
- `Ik wil thuis blijven.`
- `Ik wil een nieuwe fiets kopen.`

That kind of repetition helps grammar become automatic.
