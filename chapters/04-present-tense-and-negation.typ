= Present tense and negation

The present tense is enough to say a lot in Dutch. Once you can combine it with `niet` and `geen`, you can already build useful everyday sentences.

== Regular present tense

Most Dutch dictionary forms end in `-en`: `werken`, `leren`, `wonen`. This basic dictionary form is called the infinitive. To make the present tense, start from the stem, which is the base part that remains after removing the `-en`.

- `werken` -> stem `werk`
- `leren` -> stem `leer`
- `wonen` -> stem `woon`

#let werken-table = table(
  columns: 2,
  table.header[Subject][Form of `werken`],
  [ik], [werk],
  [jij / u], [werkt],
  [hij / zij / het], [werkt],
  [wij / we], [werken],
  [jullie], [werken],
  [zij / ze], [werken],
)

#figure(caption: [Present tense of `werken`], werken-table)

After the verb, `jij` often drops the written `-t` in questions or inversion:

- `Werk jij morgen?`
- `Morgen werk jij thuis.`

== Two very common irregular verbs

#let irregular-table = table(
  columns: 3,
  table.header[Subject][`hebben`][`zijn`],
  [ik], [heb], [ben],
  [jij / u], [hebt], [bent],
  [hij / zij / het], [heeft], [is],
  [wij / we], [hebben], [zijn],
  [jullie], [hebben], [zijn],
  [zij / ze], [hebben], [zijn],
)

#figure(caption: [Two essential irregular verbs], irregular-table)

These two verbs appear everywhere:

- `Ik heb tijd.`
- `Wij zijn thuis.`
- `Hij heeft een fiets.`

== `niet` and `geen`

This part of Dutch grammar is called negation. Negation means making a sentence or part of a sentence negative.

Use `geen` with nouns that have no article or after `een` when you mean *no* or *not a*.

- `Ik heb geen auto.`
- `Wij drinken geen koffie.`
- `Hij is geen student.`

Use `niet` to negate adjectives, verbs, adverbs, prepositional phrases, and definite nouns.

- `Ik woon niet in Den Haag.`
- `Het boek is niet duur.`
- `Wij werken vandaag niet.`
- `Ik lees het boek niet.`

At first, the exact position of `niet` can feel slippery. The big beginner rule is simple: use `geen` with indefinite nouns, and use `niet` everywhere else.

== Other common negative words

Dutch also uses a few very common negative words that replace more positive words.

#let negative-words-table = table(
  columns: 3,
  table.header[Positive word][Negative word][Meaning],
  [iemand], [niemand], [someone -> no one],
  [iets], [niets / niks], [something -> nothing],
  [ergens], [nergens], [somewhere -> nowhere],
  [ooit], [nooit], [ever -> never],
)

#figure(caption: [Common negative word pairs], negative-words-table)

Examples:

- `Er is iemand thuis.` -> someone is at home
- `Er is niemand thuis.` -> no one is at home
- `Ik zie iets.` -> I see something
- `Ik zie niets.` / `Ik zie niks.` -> I see nothing
- `Wij gaan ergens heen.` -> we are going somewhere
- `Wij gaan nergens heen.` -> we are going nowhere
- `Hij komt ooit terug.` -> he will come back sometime
- `Hij komt nooit terug.` -> he never comes back

`niks` is a very common informal form of `niets`.
