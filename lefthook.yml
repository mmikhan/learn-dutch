pre-commit:
  parallel: true
  jobs:
    - name: format code
      file_types: not executable
      stage_fixes: true
      run: bunx prettier {staged_files} --write

# Build commit messages
prepare-commit-msg:
  commands:
    commitzen:
      interactive: true
      run: '[ "{2}" = "commit" ] && exit 0; bun run cz --hook # Or npx cz --hook'
      env:
        LEFTHOOK: 0

# Validate commit messages
commit-msg:
  commands:
    "lint commit message":
      run: bun run commitlint --edit {1}
